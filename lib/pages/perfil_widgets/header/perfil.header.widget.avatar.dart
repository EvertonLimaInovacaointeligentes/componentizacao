import 'package:flutter/material.dart';
class PerfilHeaderWidgetAvatar extends StatelessWidget {
  const PerfilHeaderWidgetAvatar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const CircleAvatar(
      radius: 45,
    );
  }
}
